export default function getContent(obj) {

    let props = [
        ["method", ["GET", "POST", "PUT", "PATCH", 'DELETE']],
        ["service", []],
        ["auto inject data", "", "checkbox"]
    ];
    
    // in development

    return props;
}